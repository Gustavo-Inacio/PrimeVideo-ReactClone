import styled from 'styled-components';

const UlMovieList = styled.ul`
    display: flex;
    gap: 15px;
    flex-wrap: nowrap;
    flex-direction: row;
    width: 100%;
`;

export default UlMovieList;